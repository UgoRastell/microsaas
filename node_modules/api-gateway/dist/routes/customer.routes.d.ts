import { FastifyInstance, FastifyPluginOptions } from 'fastify';
/**
 * Customer routes
 */
export default function customerRoutes(fastify: FastifyInstance, _options: FastifyPluginOptions): Promise<void>;
